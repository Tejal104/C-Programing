#include<stdio.h>
int main()
{
	int a, b, temp;

	printf("Enter value of a :");
	scanf("%d", &a);

	printf("Enter value of b :");
	scanf("%d", &b);

	printf("Before swaping : %d %d \n ", a, b);

	temp = a;
	a = b;
	b = temp;

	printf("After swaping : %d %d ", a, b);

	return 0;
}
